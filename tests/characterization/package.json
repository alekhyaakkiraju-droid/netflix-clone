{
  "name": "netflix-characterization-tests",
  "version": "1.0.0",
  "description": "Characterization tests that document the current Spring Boot API contract",
  "scripts": {
    "test": "jest --testTimeout=10000",
    "test:ci": "jest --ci --testTimeout=10000"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/*.test.js"]
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/supertest": "^6.0.0",
    "cross-fetch": "^4.0.0",
    "dotenv": "^16.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  }
}
